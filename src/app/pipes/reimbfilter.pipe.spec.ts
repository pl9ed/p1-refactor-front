import { ReimbfilterPipe } from './reimbfilter.pipe';

describe('ReimbfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReimbfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
